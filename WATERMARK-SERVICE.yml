

eureka:
  client:
    service-url:
      default-zone: http://EUREKA-SERVER
spring:   
  datasource:
    url: jdbc:mysql://localhost:3306/imagedb
    username: root
    password: root
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    show-sql: true 
    hibernate: 
      ddl-auto: update
      
path:
  values: C:\\Users\\MohammedRinishA\\Pictures\\Downloads\\
  
command:  " -font georgia -gravity Center -pointsize 40 -fill white -evaluate multiply 0.35 -annotate -30x-30 "
  


  
  
